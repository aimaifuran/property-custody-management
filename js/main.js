// main.js

document.addEventListener('DOMContentLoaded', () => {
    // Initialize components or load data that should be present on all pages
    // For example, if you have a global navigation or header.

    // If you're using DataTables for multiple tables, you might have a generic function here.
    // For now, the DataTables initialization is within loadPurchaseOrders.

    // If you have authentication, you might check it here.
    // Uncomment the line below if you have implemented authentication logic in auth.js
    // checkAuthStatus(); // Call the corrected function from auth.js
});